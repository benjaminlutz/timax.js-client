(function () {
    'use strict';

    /**
     * Project controller.
     */
    angular.module('timax.controllers.project', [])

        .controller('ProjectController', function ($scope) {
            $scope.check = true;
        });
})();